---
alwaysApply: true
---

## 2. 依赖规则

- shared 核心仅可被 app / api 依赖
- shared 中新增目录或工具可被项目内部自由引用，但不得破坏核心规则
- 核心 breaking change 必须同步修改 app 和 api

---

## 3. 禁止内容（绝对不可违反）

- 核心目录出现业务逻辑、状态机或副作用
- IO / HTTP / DB / 消息队列 相关代码
- 框架依赖（React、NestJS、Axios 等）
- 核心以外目录不得随意破坏核心约束

---

## 4. AI 行为约束（最高优先级）

- 不允许为了示例完整修改核心目录内容
- 不允许绕过 shared 核心直接在业务层定义类型或常量
- 不允许破坏核心规则来适配生成内容
- 不允许使用非命名导出、直接 import 子路径或引入 runtime 依赖

违反以上任意条目即视为错误实现。
